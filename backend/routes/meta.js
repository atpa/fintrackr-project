/**
 * Meta routes
 * Handles metadata endpoints
 */

const express = require('express');
const router = express.Router();

// This is covered by currency.js now, keeping for compatibility
module.exports = router;
