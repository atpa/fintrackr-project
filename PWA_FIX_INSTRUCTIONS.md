# 🔧 PWA Fix - Исправление Service Worker

## ✅ Что исправлено

### 1. **Service Worker (public/sw.js)**

#### Обновлена версия
```javascript
const CACHE_VERSION = 'fintrackr-v1.0.2'; // было v1.0.1
```

#### Исправлена логика fetch-обработчика
**Проблема:** SW безусловно показывал "Offline - resource not available" для любых неудачных запросов, даже когда сервер работал.

**Решение:**
- ✅ Добавлена отладочная страница `/test.html` - запросы к ней **проходят напрямую** без обработки SW
- ✅ Навигационные запросы (HTML) используют **Network First с коротким таймаутом (2 сек)**
- ✅ Offline fallback показывается **ТОЛЬКО** при реальной потере сети И отсутствии в кэше
- ✅ 404/500 ошибки от сервера **возвращаются как есть**, а не заменяются на offline страницу
- ✅ Добавлены подробные логи для отладки (режим `DEBUG_MODE = true`)

#### Обновлён список кэшируемых ресурсов
**Убрано** (файлов не существует):
```javascript
'/js/dashboard.js'  // лежит в public/js/, не в корне
'/js/transactions.js'
'/js/accounts.js'
// и другие неправильные пути
```

**Добавлено** (реальные пути):
```javascript
'/js/pwa.js'        // существует в public/js/
'/js/layout.js'
'/js/app.js'
'/js/api.js'
// и другие реальные файлы
```

#### Добавлена мягкая установка
Теперь SW не ломается при установке, если какой-то файл недоступен:
```javascript
// Кэшируем по одному файлу, логируем ошибки
for (const asset of STATIC_ASSETS) {
  try {
    await cache.add(asset);
    cachedCount.success++;
  } catch (error) {
    log(`Failed to cache ${asset}:`, error.message);
    cachedCount.failed++;
  }
}
```

### 2. **Тестовая страница (public/test.html)**

Создана полнофункциональная страница для отладки PWA:

#### Возможности:
- 🧹 **Unregister SW** - удаление Service Worker
- 🗑️ **Clear All Caches** - очистка всех кэшей
- ✅ **Register SW** - регистрация нового SW
- 🔍 **Check SW Status** - проверка статуса
- 🧪 **API Tests** - тестирование endpoints (accounts, rates, dashboard)
- 📊 **System Info** - статус сервера, манифеста, онлайн/оффлайн

**Ключевая особенность:** Эта страница **не обрабатывается Service Worker** - все запросы идут напрямую к серверу.

---

## 🚀 Инструкция по применению

### Шаг 1: Открыть тестовую страницу

В браузере перейти на:
```
http://localhost:3000/test.html
```

Эта страница работает БЕЗ Service Worker и гарантированно загрузится.

### Шаг 2: Удалить старый Service Worker

На странице `/test.html` нажать кнопки **в таком порядке**:

1. **🗑️ Unregister SW** - удалит зарегистрированный Service Worker
2. **🧹 Clear All Caches** - очистит все кэши (fintrackr-v1.0.1-static, fintrackr-v1.0.1-dynamic, etc.)

**Результат:** Должны появиться сообщения:
```
✅ Service Worker unregistered
✅ Cleared N caches
```

### Шаг 3: Зарегистрировать новый Service Worker

На той же странице `/test.html` нажать:

3. **✅ Register SW** - зарегистрирует новый SW с версией v1.0.2

**Результат:**
```
✅ Service Worker registered
```

В консоли браузера (F12 → Console) должны появиться логи:
```
[SW] ✓ Service Worker script loaded fintrackr-v1.0.2
[SW] Installing Service Worker... fintrackr-v1.0.2
[SW] Caching static assets
[SW] Static assets cached: 18 success, 0 failed
[SW] Activating Service Worker... fintrackr-v1.0.2
[SW] Service Worker activated and ready
```

### Шаг 4: Проверить основные страницы

Теперь можно открыть основные страницы:

- http://localhost:3000/ (index.html)
- http://localhost:3000/dashboard.html
- http://localhost:3000/login.html
- http://localhost:3000/transactions.html

**Ожидаемый результат:** Страницы загружаются **нормально**, без "Offline - resource not available".

В консоли браузера (F12 → Console) будут видны логи:
```
[SW] Intercepting request: GET /dashboard.html
[SW] → Page request (network first): /dashboard.html
[SW] ✓ Page loaded from network: 200 /dashboard.html
```

### Шаг 5: Проверить offline режим (опционально)

1. В DevTools (F12) → Network → выбрать **Offline**
2. Обновить страницу (F5)

**Ожидаемый результат:** 
- Если страница есть в кэше → загрузится из кэша
- Если нет в кэше → покажется красивая offline заглушка:
  ```
  📡 Нет подключения
  Проверьте соединение с интернетом
  Страница: /dashboard.html
  [Попробовать снова]
  ```

---

## 🐛 Если проблема не решилась

### Вариант 1: Жёсткая очистка через DevTools

1. F12 → **Application** (или Приложение)
2. **Service Workers** → нажать **Unregister**
3. **Storage** (в левой панели) → **Clear site data**
4. **Закрыть и снова открыть** браузер
5. Перейти на http://localhost:3000/test.html
6. Нажать **✅ Register SW**

### Вариант 2: Incognito mode

1. Открыть браузер в режиме **Инкогнито** (Ctrl+Shift+N)
2. Перейти на http://localhost:3000/dashboard.html
3. Service Worker зарегистрируется с нуля (v1.0.2)

### Вариант 3: Другой браузер

Если проблема в одном браузере:
- Chrome → попробовать Edge
- Firefox → попробовать Chrome
- etc.

---

## 📊 Как проверить, что всё работает

### ✅ Признаки успешного исправления:

1. **В консоли (F12 → Console):**
   ```
   [SW] ✓ Service Worker script loaded fintrackr-v1.0.2
   [SW] Intercepting request: GET /dashboard.html
   [SW] ✓ Page loaded from network: 200 /dashboard.html
   ```

2. **На странице /test.html:**
   ```
   System Information:
   🖥️ Сервер: ✅ Работает
   ⚙️ Service Worker: ✅ Registered (Active)
   📱 PWA Manifest: ✅ Доступен
   🌐 Online: ✅ Online
   ```

3. **Страницы загружаются нормально:**
   - http://localhost:3000/ - главная
   - http://localhost:3000/dashboard.html - дашборд
   - http://localhost:3000/login.html - логин
   - **БЕЗ** сообщения "Offline - resource not available"

### ❌ Признаки проблемы:

1. В консоли старая версия:
   ```
   [SW] Service Worker script loaded fintrackr-v1.0.1  ← СТАРАЯ!
   ```
   **Решение:** Повторить Шаг 2 (Unregister + Clear Caches)

2. Страницы показывают offline заглушку:
   ```
   Offline - resource not available
   ```
   **Решение:** Проверить версию SW в консоли, повторить Вариант 1

3. В DevTools (F12 → Application → Service Workers) показывает:
   ```
   Status: waiting to activate
   ```
   **Решение:** Нажать "skipWaiting" или закрыть все вкладки с сайтом

---

## 🔍 Отладочные логи

### Включены по умолчанию (DEBUG_MODE = true)

В консоли будут видны:

**Установка:**
```
[SW] Installing Service Worker... fintrackr-v1.0.2
[SW] Caching static assets
[SW] Static assets cached: 18 success, 0 failed
```

**Активация:**
```
[SW] Activating Service Worker... fintrackr-v1.0.2
[SW] Deleting old cache: fintrackr-v1.0.1-static
[SW] Service Worker activated and ready
```

**Запросы:**
```
[SW] Intercepting request: GET /dashboard.html
[SW] → Page request (network first): /dashboard.html
[SW] ✓ Page loaded from network: 200 /dashboard.html
[SW] ✓ Cached new resource: http://localhost:3000/dashboard.html
```

**API:**
```
[SW] Intercepting request: GET /api/accounts
[SW] → API request (network first): /api/accounts
[SW] ✓ API response from network: 200
[SW] ✓ Cached API response
```

**Ошибки (если есть):**
```
[SW] ✗ Page network request failed: TypeError: Failed to fetch
[SW] ✓ Serving page from cache: /dashboard.html
```

### Выключить логи (для продакшена)

В файле `public/sw.js` изменить:
```javascript
const DEBUG_MODE = false; // было true
```

---

## 📝 Технические детали изменений

### Файл: `public/sw.js`

**1. Версия:** v1.0.1 → **v1.0.2**

**2. Добавлены константы:**
```javascript
const DEBUG_MODE = true;
const DEBUG_PAGES = ['/test.html', '/test-auth.html'];
```

**3. Изменён список STATIC_ASSETS:**
- Удалены несуществующие пути (`/js/dashboard.js` и т.д.)
- Добавлены реальные пути (`/js/pwa.js`, `/js/layout.js` и т.д.)

**4. Установка теперь не ломается на ошибках:**
```javascript
for (const asset of STATIC_ASSETS) {
  try {
    await cache.add(asset);
  } catch (error) {
    log(`Failed to cache ${asset}:`, error.message);
  }
}
```

**5. Fetch-обработчик:**
```javascript
// Bypass для отладочных страниц
if (DEBUG_PAGES.some(page => url.pathname === page)) {
  event.respondWith(fetch(request));
  return;
}

// Network First для HTML
if (request.mode === 'navigate' || url.pathname.endsWith('.html')) {
  event.respondWith(networkFirstWithFallback(request, ...));
  return;
}
```

**6. Новая стратегия `networkFirstWithFallback`:**
- Таймаут 2 секунды (было 5)
- Если сервер вернул 404/500 → **возвращается реальная ошибка** (не offline страница)
- Offline fallback показывается **ТОЛЬКО** при `TypeError` (нет сети) И отсутствии в кэше

### Файл: `public/test.html`

Создан с нуля. Основные функции:
- `unregisterSW()` - удаление SW
- `clearAllCaches()` - очистка кэшей
- `registerSW()` - регистрация SW
- `checkSWStatus()` - проверка статуса
- `testAccounts()`, `testRates()`, `testDashboard()` - тесты API

---

## 💡 Дополнительные рекомендации

### 1. Проверка в разных браузерах
- ✅ Chrome/Edge - полная поддержка PWA
- ✅ Firefox - хорошая поддержка
- ⚠️ Safari - частичная поддержка (особенно на iOS)

### 2. HTTPS в продакшене
Service Worker требует HTTPS (кроме localhost). При деплое убедиться, что:
- Сайт работает по HTTPS
- Сертификат валидный
- `COOKIE_SECURE=true` в .env

### 3. Версионирование
При изменении SW в будущем:
- Увеличивать `CACHE_VERSION` (v1.0.2 → v1.0.3)
- Старые кэши автоматически удаляются при активации

### 4. Production режим
Перед деплоем:
```javascript
const DEBUG_MODE = false;  // выключить логи
const DEBUG_PAGES = [];    // убрать отладочные страницы
```

---

## 🎯 Итоговый чеклист

- [ ] Открыл http://localhost:3000/test.html
- [ ] Нажал "Unregister SW" → увидел "✅ Service Worker unregistered"
- [ ] Нажал "Clear All Caches" → увидел "✅ Cleared N caches"
- [ ] Нажал "Register SW" → увидел "✅ Service Worker registered"
- [ ] Открыл http://localhost:3000/dashboard.html → страница загрузилась нормально
- [ ] Открыл F12 → Console → увидел логи с версией **v1.0.2**
- [ ] Открыл http://localhost:3000/login.html → страница загрузилась нормально
- [ ] Нажал F5 (обновить) → страница загрузилась нормально (без offline заглушки)

**Если все пункты выполнены → PWA работает корректно! ✅**

---

## 📞 Troubleshooting

### Проблема: "Service Worker not supported"
**Причина:** Старый браузер  
**Решение:** Обновить браузер или использовать Chrome/Edge/Firefox последней версии

### Проблема: SW регистрируется, но страницы не загружаются
**Причина:** Кэш содержит старые битые ссылки  
**Решение:** 
1. F12 → Application → Service Workers → Unregister
2. F12 → Application → Storage → Clear site data
3. **Закрыть ВСЕ вкладки** с localhost:3000
4. Открыть заново

### Проблема: В консоли ошибки "Failed to cache /js/..."
**Причина:** Файл не существует в STATIC_ASSETS  
**Решение:** Убрать этот путь из STATIC_ASSETS или создать файл

### Проблема: Offline fallback показывается в онлайне
**Причина:** Таймаут слишком короткий или сервер медленно отвечает  
**Решение:** Увеличить таймаут в `networkFirstWithFallback`:
```javascript
const timeoutId = setTimeout(() => controller.abort(), 5000); // было 2000
```

---

**Версия документа:** 1.0  
**Дата:** 2025-11-14  
**Автор:** GitHub Copilot
