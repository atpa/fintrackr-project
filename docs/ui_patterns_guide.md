# 🎨 UI Patterns Guide — FinTrackr

> **Дата создания**: 2025  
> **Цель**: Документирование установленных UI-паттернов для обеспечения консистентности при разработке новых функций

## 📊 Обзор используемых паттернов

FinTrackr использует **3 основных UI-паттерна** для отображения данных:

1. **Адаптивный паттерн** (cards + table fallback)
2. **Список-карточки** (list-items с card styling)
3. **Таблицы** (classic data tables)

---

## 1️⃣ Адаптивный паттерн: Cards + Hidden Table

### Где используется
- `accounts.html` — список счетов/кошельков

### Структура HTML
```html
<!-- Грид с карточками (основной вид) -->
<div id="accountsList" class="wallet-grid" aria-live="polite">
  <div class="wallet-card">
    <div class="wallet-header">
      <div class="wallet-icon">💼</div>
      <div class="wallet-name">Сбербанк</div>
    </div>
    <div class="wallet-balance">12,500.00 <span class="currency">RUB</span></div>
  </div>
</div>

<!-- Скрытая таблица (fallback/accessibility) -->
<table id="accountsTable" class="visually-hidden" aria-hidden="true">
  <thead>
    <tr>
      <th>Название</th>
      <th>Валюта</th>
      <th>Баланс</th>
    </tr>
  </thead>
  <tbody>...</tbody>
</table>
```

### CSS классы
- `.wallet-grid` — CSS Grid (1-3 колонки в зависимости от ширины экрана)
- `.wallet-card` — карточка с тенью, rounded corners, hover-эффектами
- `.visually-hidden` — скрывает таблицу (остаётся доступной для screen readers)

### Когда использовать
- ✅ Небольшое количество полей (2-4)
- ✅ Нужна визуальная привлекательность (dashboard-like view)
- ✅ Пользователи используют desktop/tablet (mobile не критично)

---

## 2️⃣ Список-карточки: List Items

### Где используется
- `transactions.html` — список транзакций
- `goals.html` — список финансовых целей (`.goals-grid` → `.goal-card`)

### Структура HTML (transactions)
```html
<div id="transactionsList" class="tx-list" aria-live="polite">
  <div class="tx-item">
    <div class="tx-main">
      <span class="tx-title">Продуктовый магазин</span>
      <span class="tx-meta">2025-01-15 • Продукты</span>
    </div>
    <div class="tx-category">
      <span class="chip">🍔 Еда</span>
    </div>
    <div class="tx-amount expense">-1,200.00 RUB</div>
    <div class="tx-actions">
      <button class="btn-icon" title="Редактировать">✏️</button>
      <button class="btn-icon" title="Удалить">🗑️</button>
    </div>
  </div>
</div>
```

### CSS классы
- `.tx-list` — flex/grid контейнер для вертикального списка
- `.tx-item` — строка-карточка с grid layout (4 колонки: main, category, amount, actions)
- `.chip` / `.tx-category-pill` — бейджи для категорий

### Когда использовать
- ✅ Средне-большое количество записей (10-100+)
- ✅ Нужна пагинация
- ✅ Смешанные типы контента (текст + бейджи + кнопки)
- ✅ Важна мобильная адаптивность (stack columns на mobile)

---

## 3️⃣ Таблицы: Data Tables

### Где используется
- `budgets.html` — таблица бюджетов с progress bars
- `categories.html` — CRUD-операции с категориями
- `planned.html` — планируемые операции
- `rules.html` — автоматические правила обработки транзакций

### Структура HTML
```html
<table id="budgetsTable">
  <thead>
    <tr>
      <th>Категория</th>
      <th>Месяц</th>
      <th>Лимит</th>
      <th>Потрачено</th>
      <th>Процент</th>
      <th>Действия</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>🍔 Еда</td>
      <td>2025-01</td>
      <td>50,000.00 RUB</td>
      <td>32,150.00 RUB</td>
      <td>
        <div class="progress-bar">
          <div class="progress-fill" style="width: 64.3%"></div>
        </div>
        64.3%
      </td>
      <td>
        <button class="btn-icon">✏️</button>
        <button class="btn-icon">🗑️</button>
      </td>
    </tr>
  </tbody>
</table>
```

### CSS классы
- `table` (стандартный HTML tag) — стилизован в `style.css` (lines ~1150-1200)
- `.progress-bar` / `.progress-fill` — кастомные прогресс-бары внутри ячеек

### Когда использовать
- ✅ Табличные данные с числовыми колонками
- ✅ Нужна сортировка по колонкам (будущая функциональность)
- ✅ CRUD-операции с простыми формами
- ✅ Desktop-first подход (mobile не приоритет)

---

## 🎯 Рекомендации по выбору паттерна

### Новая функция: Отображение списка подписок
**Рекомендация**: 📋 Список-карточки (паттерн 2)  
**Причина**: Подписки содержат разнородные данные (название, цена, дата следующего платежа, статус), карточки обеспечат лучшую читаемость на mobile

### Новая функция: Финансовый календарь (grid view)
**Рекомендация**: 🔀 Адаптивный паттерн (паттерн 1)  
**Причина**: Календарная сетка естественно вписывается в CSS Grid, можно добавить fallback-таблицу для accessibility

### Новая функция: Админ-панель (управление пользователями)
**Рекомендация**: 📊 Таблицы (паттерн 3)  
**Причина**: Админ-панель desktop-only, таблица обеспечит быстрый просмотр и редактирование множества полей

---

## 🛠️ CSS Design System

### Spacing Variables
```css
:root {
  --space-0-5: 2px;    /* микро-отступы */
  --space-1: 4px;      /* маленькие */
  --space-1-5: 6px;    /* button padding */
  --space-2: 8px;      /* стандартные */
  --space-2-5: 10px;   /* chips/pills */
  --space-3: 12px;     /* средние */
  --space-3-5: 14px;   /* list items */
  --space-4: 16px;     /* большие */
  --space-4-5: 18px;   /* hero buttons */
  --space-5: 20px;     /* sections */
  --space-6: 24px;     /* card padding */
}
```

### Color Variables
```css
:root {
  --primary: #007BFF;          /* основной синий */
  --primary-light: #4A9DFF;    /* светлый синий (hover) */
  --primary-dark: #0056b3;     /* тёмный синий (active) */
  --accent: #FF6B6B;           /* акцентный красный (alerts) */
  --success: #28a745;          /* зелёный (success states) */
  --warning: #FFC107;          /* жёлтый (warnings) */
  --danger: #DC3545;           /* красный (danger) */
  --text: #212529;             /* основной текст */
  --text-secondary: #6c757d;   /* вторичный текст */
  --text-muted: #adb5bd;       /* приглушённый текст */
  --bg-color: #f8f9fa;         /* фон приложения */
  --bg-muted: #e9ecef;         /* приглушённый фон */
  --bg-subtle: #f1f3f5;        /* тонкий фон (hover) */
  --border-color: #dee2e6;     /* стандартные border */
  --border-light: #e9ecef;     /* светлые border */
}
```

### Border Radius
```css
:root {
  --radius-sm: 4px;     /* маленькие элементы */
  --radius-md: 8px;     /* карточки, кнопки */
  --radius-lg: 12px;    /* модальные окна */
  --radius-full: 9999px; /* pills, badges */
}
```

---

## ✅ Чек-лист: Добавление новой UI-страницы

1. **Определить тип данных**: список, грид, таблица
2. **Выбрать паттерн** из 3 описанных выше
3. **Использовать существующие CSS-классы** (не создавать новые без необходимости)
4. **Обеспечить accessibility**:
   - Добавить `aria-live="polite"` для динамических списков
   - Использовать `role="status"` для loading states
   - Добавить `aria-hidden="true"` для декоративных элементов
5. **Реализовать пагинацию** (если записей > 10):
   - Использовать `public/js/utils/pagination.js`
   - Добавить `.table-controls` с `.table-pagination`
6. **Протестировать адаптивность**:
   - Desktop (1920px) — основной вид
   - Tablet (768px) — адаптация grid/flex
   - Mobile (375px) — stack layout

---

## 📚 Дополнительные ресурсы

- `public/css/style.css` — полная дизайн-система (2414 строк)
- `frontend/modules/navigation.js` — sidebar/navigation компонент
- `public/js/utils/pagination.js` — реюзабельная пагинация (89 строк)
- `public/js/utils/validation.js` — валидация форм (125 строк)
- `public/js/utils/common.js` — общие утилиты (212 строк ES6)

---

**Последнее обновление**: 2025-01-XX  
**Авторы**: FinTrackr Development Team
